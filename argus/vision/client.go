// Code generated by Fern. DO NOT EDIT.

package vision

import (
	context "context"

	argus "github.com/axilioai/platform-go/argus"
	core "github.com/axilioai/platform-go/argus/core"
	internal "github.com/axilioai/platform-go/argus/internal"
	option "github.com/axilioai/platform-go/argus/option"
)

type Client struct {
	WithRawResponse *RawClient

	options *core.RequestOptions
	baseURL string
	caller  *internal.Caller
}

func NewClient(options *core.RequestOptions) *Client {
	return &Client{
		WithRawResponse: NewRawClient(options),
		options:         options,
		baseURL:         options.BaseURL,
		caller: internal.NewCaller(
			&internal.CallerParams{
				Client:         options.HTTPClient,
				MaxAttempts:    options.MaxAttempts,
				DisableRetries: options.DisableRetries,
			},
		),
	}
}

func (c *Client) Detect(
	ctx context.Context,
	request *argus.InferenceRequest,
	opts ...option.RequestOption,
) (*argus.InferenceResponse, error) {
	response, err := c.WithRawResponse.Detect(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

func (c *Client) Locate(
	ctx context.Context,
	request *argus.LocateRequest,
	opts ...option.RequestOption,
) (*argus.LocateResponse, error) {
	response, err := c.WithRawResponse.Locate(
		ctx,
		request,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}

// List every model Argus supports, with pricing.
//
// Two families: the curated VLMs served by /vision/locate (per-token
// pricing; pass their id as `model`) and the Axilio model line behind
// /vision/detect (per-page pricing; selected via `ocr_engine` /
// `inference_type` — lite is the free engine, pro is premium).
//
// Public: no API key required (it's a catalog of model names and public
// prices, nothing sensitive), so a client can discover supported models
// before it holds credentials. The SDK fetches this once, caches it, and
// validates find(model=...) locally so a typo fails fast with a clean
// error instead of a 400 from /locate.
func (c *Client) ListModels(
	ctx context.Context,
	opts ...option.RequestOption,
) (*argus.SupportedModelsResponse, error) {
	response, err := c.WithRawResponse.ListModels(
		ctx,
		opts...,
	)
	if err != nil {
		return nil, err
	}
	return response.Body, nil
}
